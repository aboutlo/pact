{
  "name": "pac-a",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "pathfinding": "^0.4.18",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-hammerjs": "^1.0.1",
    "react-scripts": "1.0.14",
    "styled-components": "^2.2.2"
  },
  "now": {
    "alias": "pacman"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "now:deploy": "yarn run build && rm -f build/static/js/*.js.map && now build --public",
    "now:alias": "now alias build-ovczggfwpr.now.sh pacman"
  },
  "devDependencies": {
    "now": "^8.5.4",
    "prettier": "^1.7.4"
  }
}
